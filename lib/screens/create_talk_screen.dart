import 'package:flutter/material.dart';
import 'create_talk_dialog.dart';

class CreateTalkScreen extends StatelessWidget {
  const CreateTalkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const CreateTalkDialog();
  }
}
